import React from "react";
import styles from "./wake_up_time.module.css";

const WakeUpTime = (props) => {
  return (
    <section className={styles.wakeup}>
      <h1>wakeUpTime</h1>
    </section>
  );
};

export default WakeUpTime;
